import { Timer } from '..'

describe('Timer COmponent', () => {
  it('is truthy', () => {
    expect(Timer).toBeTruthy()
  })

  it('testing seconds', () => {
    // const timerComp = mount();
  })
})
